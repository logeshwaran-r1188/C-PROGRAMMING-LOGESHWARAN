#include <stdio.h>

int main() {
    int n, i, count = 0;
if (scanf("%d", &n) != 1) return 0;
int arr[n];
for (i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
        
        if (arr[i] % 2 == 0) {
            count++;
        }
    }

   
    printf("%d\n", count);

    return 0;
}
