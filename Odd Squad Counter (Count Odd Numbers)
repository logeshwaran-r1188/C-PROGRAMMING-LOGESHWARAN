#include <stdio.h>

int main() {
    int N, count = 0;

    if (scanf("%d", &N) != 1) return 0;

    for (int i = 0; i < N; i++) {
        int soldier;
        scanf("%d", &soldier);

        if (soldier % 2 != 0) {
            count++;
        }
    }

    printf("%d\n", count);

    return 0;
}
